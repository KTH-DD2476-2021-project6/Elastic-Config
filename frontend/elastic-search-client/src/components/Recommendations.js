import LinkButton from "./LinkButton";

const Recommendations = ({ recommendations }) => {
    return (
        <div>
            <LinkButton to="/" className="btn btn-sm btn-warning border-black">Back</LinkButton>
        </div>
    )
}

export default Recommendations
